<div class="max-w-3xl mx-auto px-4">
  <div class="flex items-center justify-between">
    <h1 class="flex-1 font-bold text-3xl">Blazer</h1>

    <div class="flex-wrap lg:flex items-center lg:space-x-3">
      <%= link_to blazers_path, class: button_class(theme: "transparent") do %>
      All Blazers
      <% end %>
      <%= link_to edit_blazer_path(@blazer), class: button_class(theme:"primary") do %>
      Edit
      <% end %>
    </div>
  </div>

  <ul class="text-slate-700">
  </ul>
</div>
